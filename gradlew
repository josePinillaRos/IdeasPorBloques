#!/usr/bin/env sh
# Simplified gradlew script placeholder. Android Studio can use its own Gradle to import and regenerate the wrapper.
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/gradlew.bat" "$@"
